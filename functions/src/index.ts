// Cloud Functions barrel — each function lives in its own module under src/
// and is re-exported from here so the Firebase deploy picks it up via lib/index.js.
//
// Current functions:
//   (none yet — chatWithSystem lands in the Tier 0.1 implementation commit)

export {};
